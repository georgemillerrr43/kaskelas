# ── Database Configuration ──
# Copy this file to .env and fill in your values.
# Jika menggunakan .env, pastikan vlucas/phpdotenv ter-install
# atau set environment variables langsung di web server.

DB_HOST=localhost
DB_NAME=db_kas_kelas
DB_USER=root
DB_PASS=
